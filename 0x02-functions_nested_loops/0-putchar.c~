#include <stdio.h>

/**
 * main - prints _putchar, followed by a new line.
 *
 * Return: 0
 */
int main(void)
{
	char *sh = "Holberton";

	while (*sh)
	{
		_putchar(*sh);
		sh++;
	}
	_putchar('\n');

	return (0);
}
